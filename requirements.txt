anthropic>=0.30.0
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
httpx>=0.27.0
pydantic>=2.7.0
pydantic-settings>=2.3.0
sqlalchemy>=2.0.30
aiosqlite>=0.20.0
python-dotenv>=1.0.0
facebook-business>=20.0.0
beautifulsoup4>=4.12.0
lxml>=5.2.0
rich>=13.7.0
typer>=0.12.0
aiofiles>=23.2.1
jinja2>=3.1.4
tenacity>=8.3.0
python-multipart>=0.0.9
# Stripe — checkout & payment links
stripe>=9.0.0
# Playwright — JavaScript-rendered page scraping (TikTok, AliExpress)
playwright>=1.44.0
# yt-dlp — TikTok video metadata extraction (no login needed)
yt-dlp>=2024.5.1
# ngrok SDK — programmatic tunnel for landing pages
pyngrok>=7.1.0
# Scheduling — run cycles on cron without n8n
apscheduler>=3.10.4
# Pillow — image processing for ad creatives
Pillow>=10.3.0
# Requests — sync fallback for some SDKs
requests>=2.32.0
# Resend — transactional email (operator alerts + customer confirmations)
resend>=2.0.0
